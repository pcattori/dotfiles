- defaults:
    link:
      create: true
      relink: true

- clean: ["~"]

- link:
    "~/Library/Application Support/Code/User/settings.json": vscode/settings.json
    ~/.config/nvim: nvim
    ~/.gitconfig: gitconfig
    ~/.ssh/config: ssh/config
    ~/.zsh: zsh
    ~/.zshrc: zshrc
