- defaults:
    link:
      create: true
      relink: true

- clean: ["~"]

- link:
    ~/.gitconfig: gitconfig
    # ~/.config/nvim: nvim
    ~/.ssh/config: ssh/config
    "~/Library/Application Support/Code/User/settings.json": vscode/settings.json
    ~/.zsh: zsh
    ~/.zshrc: zshrc
